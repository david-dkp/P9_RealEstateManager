<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/add_estate_item"
        android:icon="@drawable/ic_add"
        android:title="@string/item_add_estate_title"
        app:iconTint="?attr/colorControlNormal"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/search_estate_item"
        android:icon="@drawable/ic_search"
        android:title="@string/item_search_estate_title"
        app:iconTint="?attr/colorControlNormal"
        app:showAsAction="ifRoom"

        />

    <item
        android:id="@+id/edit_estate_item"
        android:icon="@drawable/ic_edit"
        android:title="@string/item_edit_estate_title"
        app:iconTint="?attr/colorControlNormal"
        app:showAsAction="ifRoom" />

</menu>